# Starter Proof

The starter proof is:

`safe fixture -> normalized bundle -> keyword index -> semantic index -> graph -> answer packet`

It proves the connector's minimum loop without live credentials.

The Stepik clean API proof adds:

`Stepik-shaped raw API fixture -> normalized bundle -> index -> graph -> answer packet`

The hard-adapter proof adds:

`GetCourse/Skillspace browser snapshot fixture -> normalized bundle -> index -> graph -> answer packet`
