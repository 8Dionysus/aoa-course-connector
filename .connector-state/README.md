# Local Connector State

This directory is a tiny committed scaffold for local connector state. Generated
data inside it is ignored by Git.

- `data/`: normalized runs and source registry
- `cache/`: regenerable cache
- `auth/`: local auth-state manifests or browser storage-state files
- `artifacts/`: indexes, graphs, answer packets, reports
