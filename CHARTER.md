# Charter

`aoa-course-connector` exists so agents can use the operator's legitimate course
access as a durable knowledge source.

The repository should grow toward:

- deep course discovery across supported platforms;
- normalized course schemas independent of platform quirks;
- local indexes and graphs that remain rebuildable;
- query packets with precise lesson evidence;
- install and MCP routes that a fresh agent can follow.
